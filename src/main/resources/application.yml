logging:
  level:
    com.lucasmedeiros.creditengine: DEBUG
    org.springframework.web: INFO
  file:
    name: logs/credit-engine.log

springdoc:
  swagger-ui:
    path: /swagger
    operations-sorter: method
    tags-sorter: alpha
    try-it-out-enabled: true